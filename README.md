# 🌸 Cute Calendar

A cozy pixel‑style desktop calendar app built with **Electron**, **HTML**, **CSS**, and **JavaScript**.

--- 

## ✨ Features 
- Frameless Electron window with rounded corners
- Pixel‑style fonts for a retro aesthetic
- Custom background frames (`outer-frame-bg.png`, `inner-frame-bg.png`)
- Displays the current **day** and **month**
- Lightweight and beginner‑friendly codebase
  
---

## 📂 What’s in this repo
| File / Folder      | Purpose |
|--------------------|---------|
| `index.html`       | Main HTML layout for the calendar UI |
| `styles.css`       | All styles for the calendar interface |
| `script.js`        | Frontend JavaScript controlling calendar logic |
| `main.js`          | Electron main process — creates the desktop window and loads your UI |
| `package.json`     | Project metadata & dependencies |
| `.gitignore`       | Files and folders ignored by Git |
| `assets/`          | Images, fonts, and other static assets |
| `package-lock.json`| Auto‑generated lock file for dependencies |

---

## 🚀 How to use

### 1. Clone this repo
```bash
git clone https://github.com/manal312/cute-calendar.git
cd cute-calendar
```

### 2. Install Node.js
Make sure you have Node.js  installed , then run:
```bash
npm install
```
This installs Electron and any other modules required to run the app.

### 3. Run the app locally
```bash
npm start
```
This will open your Cute Calendar as a desktop application powered by Electron.

---

## 💡 Future Ideas
- Add year display
- Theme switching (light/dark)
- Animated transitions
- Export calendar as image


